services:
  discipline:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DB_FILE: /data/discipline.sqlite
    volumes:
      - discipline-data:/data
    restart: unless-stopped

volumes:
  discipline-data:
